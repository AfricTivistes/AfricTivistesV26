---
import PageHero from "@/components/PageHero.astro";
import { extractImages, type Lang } from "@/lib/i18n";
import valuesData from "@/data/translations/values.json";

interface Props { lang: Lang; }
const { lang } = Astro.props;
const imgs = extractImages(valuesData);
---
<PageHero
  lang={lang}
  backgroundImage={imgs._img_heroImage}
  labelKey="nav.about.values"
  titleKey="values.title"
  subtitleKey="values.subtitle"
  gradient="bg-linear-to-br from-primary/95 via-primary/80 to-[hsl(37,70%,30%)]/80"
  patternOpacity="opacity-15"
  verticalPadding="py-12 lg:py-16"
>
  <div class="absolute top-20 right-[15%] w-32 h-32 border-2 border-secondary/20 rounded-full hidden lg:block animate-float"></div>
  <div class="absolute bottom-16 left-[10%] w-20 h-20 border border-white/10 rounded-xl rotate-12 hidden lg:block"></div>
  <div class="a-grow-x h-1 bg-secondary rounded-full mt-8" style="--a-delay:400ms"></div>
</PageHero>
